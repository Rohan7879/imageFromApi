import { FetchPipePipe } from './fetch-pipe.pipe';

describe('FetchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FetchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
